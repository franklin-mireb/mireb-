services:
  - type: web
    name: mireb-crm
    env: node
    plan: free
    branch: main
    rootDir: .
    buildCommand: "npm install"
    startCommand: "npm start"
    envVars:
      - key: NODE_VERSION
        value: "18.17.1"
