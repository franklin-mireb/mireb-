{
  "name": "mireb-crm",
  "version": "1.0.0",
  "description": "CRM Mireb avec interface admin complète",
  "main": "backend/server-unified.js",
  "type": "module",
  "scripts": {
    "start": "node backend/server-unified.js",
    "dev": "node server-local.cjs",
    "build": "mkdir -p public && cp *.html public/ && cp -r api public/",
    "vercel-build": "mkdir -p public && cp *.html public/ && cp -r api public/ && echo 'Vercel build complete'"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "multer": "^1.4.5-lts.1",
    "dotenv": "^16.3.1"
  },
  "engines": {
    "node": ">=18.0.0"
  },
  "keywords": ["crm", "mireb", "admin", "api"],
  "author": "Mireb",
  "license": "MIT"
}
